# セキュリティ方針

AI Story Builder は、原稿データを利用者の端末内（IndexedDB / アプリローカルデータ）に保持する
ローカルファーストのデスクトップアプリです。サーバーを持たず、外部通信は利用者自身が設定した
AIプロバイダーへの送信のみに限られます。

## 想定する脅威と、しない脅威

| 対象 | 扱い |
| --- | --- |
| 原稿の意図しない外部送信 | **防ぐ**（接続先を検証し、許可リスト外への送信を拒否） |
| APIキーのログ・エクスポートへの混入 | **防ぐ**（保存・出力の前にマスク） |
| AI生成文・取り込み原稿によるXSS | **防ぐ**（HTMLレンダリングを行わない／サニタイズ） |
| ディスクを直接覗かれた場合のAPIキー閲覧 | **緩和のみ**（後述） |
| 端末が既にマルウェアに感染している場合 | **対象外**（アプリ側で防げない） |

## 実装上の約束

以下は変更するとセキュリティ特性が壊れるため、修正時は理由を確認してください。

- **ローカルLLMの接続先検証** — `src/utils/securityUtils.ts` の `isAllowedLocalEndpoint()`。
  ホスト名を厳密にIPv4として解析し、ループバックと私的アドレスのみ許可します。
  前方一致の文字列比較に戻すと `10.evil.com` のような公開ホストが通過し、原稿全文の送信先に
  なりえます（回帰テスト: `src/tests/utils/isAllowedLocalEndpoint.test.ts`）。
  この関数は `aiService` と `checkLocalLLMConnectivity()` の両方で通ります。

- **markdown-it の `html` オプション** — `MainEditor.tsx` / `ReviewStep.tsx` ともに `html: false`。
  ここに渡るのはAI生成文と取り込み原稿であり、サニタイザは介在しません。`true` にすると
  生のHTMLがそのまま描画されます。

- **APIキーはヘッダーでのみ送る** — URLのクエリ文字列に載せないこと。URLはエラーログに残ります。
  ログにURLを書く箇所は `httpService.ts` の `maskUrlForLog()` を必ず通します。

- **AIログのマスク** — `maskSecretsInText()` を保存前に適用。AIログはIndexedDBに永続化され、
  ファイル書き出しやデータエクスポートにも含まれます。

- **復号失敗時に暗号文を返さない（fail-closed）** — `decryptApiKeyAsync()` は、保存値が
  `v3:` / `v2:` で始まる＝暗号文と確定している場合、復号に失敗したら**空文字を返します**。
  かつてはここで暗号文をそのまま返していたため、`v3:AbCd...` という文字列が「復号済みの鍵」として
  設定画面に入り、`Authorization` ヘッダーで外部APIへ送信されていました（Gemini 400 / OpenAI 401 の
  原因）。さらにその状態で保存すると暗号文が再暗号化され、元の鍵が失われていました。
  `isEncryptedApiKey()` で送信前にも二重に防いでいます
  （回帰テスト: `src/tests/utils/apiKeyDecryption.test.ts`）。
  復号に失敗する主因は鍵導出の種の変化（localStorage の `_enc_seed_v1` の消失、v2はデバイス情報の変化）で、
  この場合ユーザーはAPIキーを再入力する必要があります。

- **テストの `crypto` を偽物にしない** — `src/tests/setup.ts` は Node の実 WebCrypto を使います。
  以前はダミー実装（`decrypt` が常にゼロ配列を返す）だったため、上記の復号失敗バグを
  テストで検出できませんでした。

- **プロンプトのサニタイズは原稿を書き換えない** — 「無視」「忘れる」等の語句削除や、
  該当語を含む行の除去は**行いません**。利用者自身の原稿を利用者自身のAPIキーで送る構造上、
  この種の削除は防御にならず、日本語の地の文を破壊するだけでした
  （回帰テスト: `src/tests/utils/promptContentPreservation.test.ts`）。
  制御文字の除去・空白の正規化・長さ制限は維持します。

- **Tauri のプラグイン権限** — `src-tauri/capabilities/default.json` が実質的な唯一の境界です
  （Rust側にカスタムコマンドはありません）。
  - ファイルの**読み取り**権限（`fs:allow-read-text-file`）は付与していません。コード上、
    プラグイン経由で読むのは `readDir` のみで、ファイル内容の読み取りは行っていないためです。
    取り込み機能はブラウザの File API（`textEncoding.ts`）を使うので、この権限は不要です。
    追加すると、SSH鍵やクラウド認証情報を読める権限を無償で渡すことになります。
  - **書き込み**は `$HOME/**` を許可しています。保存先はネイティブの保存ダイアログで
    利用者が選ぶため、ホーム配下の任意のフォルダ（例: `~/Novels/`）を弾くと保存が失敗します。
    代わりに、自動起動・シェル初期化・認証情報のパスを `deny` で塞いでいます
    （denyはallowより優先されます）。この deny 一覧は削らないでください。

## 既知の残余リスク

- **APIキーの暗号化はディスク閲覧対策まで** — AES-GCM + PBKDF2 で暗号化していますが、
  鍵の種は同じ端末の localStorage にあります。端末上でコードを実行できる攻撃者に対しては
  保護になりません。OSキーチェーン統合は今後の検討課題です。

- **`script-src 'unsafe-inline'`** — Tauri の CSP でインラインスクリプトを許可しています。
  isolation パターンおよび nonce 方式への移行は構造変更が大きく、未対応です。

- **コード署名と更新チャネルが無い** — Windowsインストーラーは未署名で、自動更新もありません
  （＝更新機構由来の攻撃面はゼロですが、修正を配布する手段もありません）。配布は手動DLです。

- **dev依存の `brace-expansion` DoS 勧告** — eslint / tailwindcss(sucrase) / typescript-eslint 経由。
  本番バンドルには含まれず、修正には eslint の破壊的更新が必要なため、当面は受容します。
  `npm audit` で件数が出ますが、いずれもこの1件に由来します。

- **Androidキーストア作成時のパスワード** — `scripts/create-android-keystore.js` は
  `keytool` に引数で渡すため、同一マシンの他ユーザーからプロセス一覧で見えます。
  共有マシンでは `keytool` を対話実行してください。

## 環境変数の注意

`VITE_` で始まる変数は**ビルド時にバンドルへ平文で埋め込まれます**。APIキーを入れないでください。
キーはアプリ内の設定画面から入力し、暗号化ストレージに保存させてください。
`.env*`（`env.local.example` を除く）はすべて `.gitignore` 対象です。
